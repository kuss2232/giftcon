<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="faq">

<select id="faqList" parameterType="hashmap" resultType="hashmap">
<!-- faq 전체리스트 보기 -->
		<![CDATA[
			SELECT 
		FAQ_NUM, 
		FAQ_TITLE, 
		FAQ_CNT, 
		FAQ_HITCOUNT, 
		FAQ_CATEGORY 
			FROM 
		FAQ 
		ORDER BY 
		FAQ_NUM DESC
		]]>
	</select>
	
	<select id="faqCategoryList" parameterType="hashmap" resultType="hashmap">
<!-- 카테고리별 FAQ 리스트 -->

		<![CDATA[
		SELECT
			FAQ_NUM, 
			FAQ_TITLE, 
			FAQ_CNT, 
			FAQ_HITCOUNT, 
			FAQ_CATEGORY 
			FROM FAQ 
	WHERE 
			FAQ_CATEGORY=#{FAQ_CATEGORY} 
	ORDER BY 
			FAQ_NUM DESC
		]]>
	</select>
	
	<!-- faq 글쓰기 -->
	<insert id="faqWrite" parameterType="hashmap">
		<![CDATA[
			INSERT INTO FAQ
				( 
				FAQ_NUM, 
				FAQ_TITLE, 
				FAQ_CNT, 
				FAQ_CATEGORY
			) 
			VALUES
			( 
				FAQ_NUM_SEQ.NEXTVAL, 
				#{FAQ_TITLE}, 
				#{FAQ_CNT}, 
				#{FAQ_CATEGORY}
			)
		]]>
	</insert>
	<!-- FAQ 조회수 카운트 -->
	<update id="faqHitcount" parameterType="hashmap">
		UPDATE FAQ 
	SET 
		FAQ_HITCOUNT = NVL(FAQ_HITCOUNT, 0) + 1 
	WHERE 
		FAQ_NUM = #{FAQ_NUM}
	</update>
	
	<!-- FAQ 수정하기 -->
	<update id="faqCategoryUpdate" parameterType="hashmap">
		UPDATE FAQ
	SET
		FAQ_TITLE = #{FAQ_TITLE},
		FAQ_CNT = #{FAQ_CONTENT},
		FAQ_CATEGORY = #{FAQ_CATEGORY}
	WHERE
		FAQ_NUM = #{FAQ_NUM}
	</update>
	
	<!-- faq 삭제하기 -->
	<delete id="faqDelete" parameterType="hashmap">
		<![CDATA[
			DELETE FAQ
			WHERE
				FAQ_NUM = #{FAQ_NUM}
		]]>
	</delete>
	
	
	<!-- faq 제목 검색 -->
	<select id="searchFaqTitle" resultType="hashmap" parameterType="hashmap">
			<![CDATA[
		SELECT 
			FAQ_NUM,
			FAQ_TITLE,
			FAQ_HITCOUNT
		FROM FAQ
		WHERE FAQ_TITLE like '%'||#{SearchKeyword}||'%'
		ORDER BY FAQ_NUM DESC
	]]>
	</select>
	<!-- FAQ 상세보기 -->
	<select id="faqDetail" resultType="hashmap" parameterType="hashmap">
	<![CDATA[
		SELECT
			FAQ_NUM,
			FAQ_TITLE,
			FAQ_CNT,
			FAQ_HITCOUNT,
			FAQ_CATEGORY	
	FROM
			FAQ
	WHERE
			FAQ_NUM = #{FAQ_NUM}
			]]>
	</select>
</mapper>