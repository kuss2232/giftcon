<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="admin">

<select id="outputData" resultType="hashmap">
	<![CDATA[
		SELECT
			TO_CHAR(DAY_CHECK, 'YYYY-MM-DD'),
			AGE,
			GOODS_NAME,
			BIG_CATEGORY,
			SMALL_CATEGORY,
			MEMBER_GENDER
		FROM
			DATATABLE1
	]]>
</select>

</mapper>